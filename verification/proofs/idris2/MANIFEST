# Idris2 proof manifest — read by scripts/check-proofs.sh idris2
# Format: <source-root>|<module-relative-to-source-root>|gated|quarantine|<note>
# gated      = must compile.  quarantine = known-broken, must keep failing.
# Ground-truthed 2026-07-17 with idris2 0.7.0 (developer/tools/opt/pack).
# 2026-08-04: quarantine emptied — all five modules repaired and promoted
# (Data.Nat imports; unrestricted witness quantities so proofs can project
# them; FieldAligned/sizeAligned restated as divisibility instead of the
# ununifiable SIsNonZero; soUnique-based handle equality; AFBCons unfolded
# past a positivity-checker limitation). See each module's doc comments.
verification/proofs/idris2|ABI/Foreign.idr|gated| the one shipped Idris2 module that compiles — real content, not a stub
verification/proofs/idris2|BlackGlider/Constitution.idr|gated| project constitutional equations: complete legal B3/S23 centre/count domain, exact chart headings, deterministic evolution, observer exclusion, and causal radius
verification/proofs/idris2|Types.idr|gated| bounded-Nat + non-empty-list well-formedness (template examples, honest ones)
verification/proofs/idris2|ABI/Layout.idr|gated| struct size/alignment/padding; alignment stated as divisibility
verification/proofs/idris2|ABI/Platform.idr|gated| per-platform pointer/int/size_t sizes with LTE bounds
verification/proofs/idris2|ABI/Pointers.idr|gated| non-null SafePtr, So-witness uniqueness, handle equality
verification/proofs/idris2|ABI/Compliance.idr|gated| C ABI compliance record over Layout + Platform
